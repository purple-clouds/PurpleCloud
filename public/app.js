// Tony & Ro app js

// Psuedo code: 

// Accessible HTML structure
    // "header"
    // form (search bar & submit)
    // footer
    // parallex.js / check for accessibility
    // font accessibility
// CSS
    // accessibility check on colours
    // CSS art clouds? 
    // animate the clouds (accessibility check)

// JS    
    // Get API 
    // Attach event listener to form
    // Link API to "search bar" ==> on submit (get search input value)
    // Button / Submit (search button on click)
    // Populating results below search bar (keep search bar) (append ul & li inside with all the information ==> with each li have button to select resource ((also if user needs to tab))! )

    // Stretch goal:

    // Create a clear/reset button ==> reference Ro Pokedex web app "clear button"
    // Day & Night mode (toggle ==> hide / unhide / look at tutorial)
